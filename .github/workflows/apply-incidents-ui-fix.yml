name: Retired incidents UI patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm retirement
        run: echo "Incidents UI patch is already present in master; no source mutation is performed."
