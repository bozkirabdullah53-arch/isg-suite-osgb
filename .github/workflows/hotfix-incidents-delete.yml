name: Retired incident delete hotfix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm retirement
        run: echo "Incident delete hotfix is already present in master; no source mutation is performed."
